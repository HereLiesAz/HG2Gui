<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true" />

    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK"  />
    <!--
      Requested at runtime, as-needed, by call.java / cntcts.java / ContactManager.kt /
      flash.java respectively - but a runtime request for a permission the manifest never
      declares is refused before the user ever sees a dialog, so declaring these here is what
      actually makes "ask when needed" possible, not optional polish.
    -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CAMERA" />

<!--
      No <permission> declarations here.

      This manifest used to define android.permission.FLASHLIGHT. That name belongs to the
      platform, and an app that redefines a permission another package already owns is
      rejected at install time with INSTALL_FAILED_DUPLICATE_PERMISSION — surfaced to the
      user as a bare "App not installed" with no reason given. Its permissionGroup,
      android.permission-group.HARDWARE_CONTROLS, no longer exists either.

      Nothing needed it: the torch is driven through CameraManager, and `flash` requests
      android.permission.CAMERA at runtime.
    -->

    <!-- features -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.flash"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.wifi"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.network"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.audio.output"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Custom.Solid">

        <provider
            android:name=".tuils.GenericFileProvider"
            android:authorities="${applicationId}.FILE_PROVIDER"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>

        <!--
          The terminal. One entry, normal lifecycle, present in recents.

          Changed from the launcher declaration this replaces:
            - dropped clearTaskOnLaunch, excludeFromRecents, stateNotNeeded, resumeWhilePausing
              and taskAffinity="" — all launcher-lifecycle tricks. A terminal belongs in
              recents and keeps its state.
            - no category.HOME is added here. This manifest never declared one, so the app was
              already only a home-screen candidate by way of the flags above, not by filter.
            - launchMode singleTask -> singleTop
            - windowSoftInputMode stateAlwaysVisible -> stateUnchanged: the suggestion tree is
              the input method, so the keyboard opens only when asked for.
        -->
        <activity
            android:name=".TerminalActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|keyboard|navigation|uiMode"
            android:documentLaunchMode="intoExisting"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="stateUnchanged|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

<receiver
            android:name=".tuils.stuff.PolicyReceiver"
            android:description="@string/admin_permission"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/policy" />

            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".managers.notifications.KeeperService"
            android:enabled="true"
            android:exported="false" />
        <service
            android:name=".managers.music.MusicService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".commands.tuixt.TuixtActivity"
            android:excludeFromRecents="true"
            android:label="@string/tuixt_label"
            android:noHistory="true"
            android:theme="@style/Custom.Solid"
            android:exported="true"
            android:windowSoftInputMode="stateAlwaysVisible|adjustResize">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".TerminalActivity" />

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.EDIT" />
                <data android:mimeType="text/*" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.EDIT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:host="*" android:pathPattern=".*\.js" android:scheme="file" />
                <data android:host="*" android:pathPattern=".*\.htm" android:scheme="file" />
                <data android:host="*" android:pathPattern=".*\.html" android:scheme="file" />
                <data android:host="*" android:pathPattern=".*\.xhtml" android:scheme="file" />
                <data android:host="*" android:pathPattern=".*\.php" android:scheme="file" />
                <data android:host="*" android:pathPattern=".*\.ini" android:scheme="file" />
                <data android:host="*" android:pathPattern=".*\.sh" android:scheme="file" />
                <data android:host="*" android:pathPattern=".*\.log" android:scheme="file" />
                <data android:host="*" android:pathPattern=".*\.db" android:scheme="file" />
                <data android:host="*" android:pathPattern=".*\.conf" android:scheme="file" />
                <data android:host="*" android:pathPattern=".*\.cfg" android:scheme="file" />
                <data android:host="*" android:pathPattern=".*\.txt" android:scheme="file" />
                <data android:host="*" android:pathPattern=".*\.xml" android:scheme="file" />
            </intent-filter>
        </activity>


        <service
            android:name=".managers.notifications.NotificationService"
            android:label="@string/notification_reader"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
        <service android:name=".managers.notifications.NotificationMonitorService" />


        <activity
            android:name=".DontPanicActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"/>


        <activity
            android:name=".GuideActivity"
            android:theme="@style/Custom.Solid"
            android:windowSoftInputMode="adjustResize"
            android:excludeFromRecents="true" />

        <activity
            android:name=".PanicActivity"
            android:theme="@style/Custom.Solid" />

    </application>

</manifest>
